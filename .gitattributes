# Append-only history file: on a merge, keep both sides' new lines instead of
# raising a conflict. Safe ONLY because every change to this file is a new line
# appended at the end — never a reword, reorder, or deletion of an existing line.
docs/CHANGELOG.md merge=union

name: Build & Push Authos Images

on:
  push:
    branches: [ "master" ]
    paths:
      - "authos-api/**"
      - "authos-frontend/**"
      - ".github/workflows/**"

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.check.outputs.backend }}
      frontend: ${{ steps.check.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: check
        run: |
          # Count changed files
          backend_changed=$(git diff --name-only HEAD~1 HEAD | grep -c '^authos-api/' || true)
          frontend_changed=$(git diff --name-only HEAD~1 HEAD | grep -c '^authos-frontend/' || true)

          force_backend=$(git log -1 --pretty=%B | grep -ci '\[force-backend\]' || true)
          force_frontend=$(git log -1 --pretty=%B | grep -ci '\[force-frontend\]' || true)

          backend=$(( backend_changed > 0 || force_backend > 0 ? 1 : 0 ))
          frontend=$(( frontend_changed > 0 || force_frontend > 0 ? 1 : 0 ))

          # Export combined outputs
          echo "backend=$backend" >> $GITHUB_OUTPUT
          echo "frontend=$frontend" >> $GITHUB_OUTPUT


      - name: Debug detect-changes outputs
        run: |
          echo "=== DETECT CHANGES OUTPUTS ==="
          echo "Backend files changed: ${{ steps.check.outputs.backend }}"
          echo "Frontend files changed: ${{ steps.check.outputs.frontend }}"
          

  debug-conditionals:
    needs: detect-changes
    runs-on: ubuntu-latest
    steps:
    - name: Debug backend evaluation
      run: |
        echo "Commit message: ${{ github.event.head_commit.message }}"
        echo "Backend output: ${{ needs.detect-changes.outputs.backend }}"
        echo "Frontend output: ${{ needs.detect-changes.outputs.frontend }}"

        if [[ "${{ needs.detect-changes.outputs.backend }}" != "0" ]]; then
          echo "Backend job WOULD run"
        else
          echo "Backend job WOULD SKIP"
        fi

        if [[ "${{ needs.detect-changes.outputs.frontend }}" != "0" ]]; then
          echo "Frontend job WOULD run"
        else
          echo "Frontend job WOULD SKIP"
        fi



  backend-build-deploy:
    needs: detect-changes
    runs-on: ubuntu-latest
#    if: |
#      contains(github.event.head_commit.message, '[force-backend]') ||
#      needs.detect-changes.outputs.backend != '0'
    steps:
      - name: Check if backend should run
        run: |
          echo "Commit message: ${{ github.event.head_commit.message }}"
          echo "Backend output: ${{ needs.detect-changes.outputs.backend }}"

          if [[ "${{ needs.detect-changes.outputs.backend }}" == "0" ]]; then
            echo "Skipping backend build"
            exit 78   # special code to skip remaining steps without failing the job
          fi
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('authos-api/**/pom.xml') }}

      - name: Build Spring Boot App
        working-directory: ./authos-api
        run: mvn -q clean package -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build & push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./authos-api
          platforms: linux/amd64
          push: true
          tags: |
            stevetosak/authos-api:latest
            stevetosak/authos-api:${{ github.run_number }}

  frontend-build-deploy:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      contains(github.event.head_commit.message, '[force-frontend]') ||
      needs.detect-changes.outputs.frontend != '0'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('authos-frontend/**/package-lock.json') }}

      - name: Build React frontend
        working-directory: ./authos-frontend
        run: |
          npm ci
          npm run build

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build & push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./authos-frontend
          platforms: linux/amd64
          push: true
          tags: |
            stevetosak/authos-ui:latest
            stevetosak/authos-ui:${{ github.run_number }}
